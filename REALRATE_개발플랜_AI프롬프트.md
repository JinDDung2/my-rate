---
title: "RealRate 6일 개발 플랜 및 단계별 AI 프롬프트"
tags: [finance-ai, hackathon, execution, realrate, prompts]
status: active
created: 2026-08-31
---

# RealRate 개발 실행 플랜 (8/31 ~ 9/6, 하루 3~4시간)

## 원칙 3가지

1. **단계는 서로 독립적이다.** 뒤가 밀려도 앞 단계 산출물은 살아있다. Day 2까지만 끝나도 CLI 데모는 나온다.
2. **AI에게 한 번에 하나만 시킨다.** "앱 만들어줘"는 실패한다. 파일 단위·함수 단위로 자른다.
3. **계산 엔진과 데이터 검수는 직접 확인한다.** 나머지는 AI에 맡겨도 된다. 이 둘이 무너지면 서비스가 거짓말을 한다.

---

## 데이터 확보

**목표** 인증키 발급 + 원본 JSON 덤프. 여기서 막히면 기획 자체를 바꿔야 하므로 **가장 먼저** 한다.

**할 일**

1. https://finlife.fss.or.kr → 오픈API → 인증키 신청 (즉시 발급)
2. `savingProductsSearch.json?auth=KEY&topFinGrpNo=020000&pageNo=1` 호출해서 **응답 필드명을 눈으로 확인**
3. 전 페이지 순회해 `data/raw/savings.json` 저장

> ⚠️ 필드명(`spcl_cnd`, `intr_rate`, `intr_rate2`, `save_trm`, `rsrv_type`, `intr_rate_type`)은 공개 문서에서 재확인되지 않았다. **실제 응답으로 검증한 뒤** 타입을 확정할 것. 다르면 그 이름을 따른다.

**완료 조건(DoD)** `data/raw/savings.json`에 상품 30건 이상, `spcl_cnd`가 비어있지 않은 상품이 20건 이상.

### AI 프롬프트

```
Next.js 프로젝트를 만들 예정이야. 먼저 데이터 수집 스크립트만 작성해줘.

목표: 금융감독원 금융상품통합비교공시 오픈API의 적금상품 API를 호출해서
전체 페이지를 순회하고 원본 JSON을 파일로 저장하는 Node.js 스크립트.

요구사항
- 파일: scripts/fetch-products.ts, tsx로 실행
- 엔드포인트: http://finlife.fss.or.kr/finlifeapi/savingProductsSearch.json
- 쿼리: auth(process.env.FSS_API_KEY), topFinGrpNo=020000, pageNo
- 응답의 result.max_page_no 만큼 페이지를 순회, 페이지 간 300ms 대기
- 모든 페이지의 result.baseList와 result.optionList를 각각 합쳐서
  data/raw/savings.json 에 { baseList, optionList, fetchedAt } 형태로 저장
- 응답 스키마를 확신할 수 없으니, 첫 페이지 응답의 baseList[0]와 optionList[0]의
  키 목록을 콘솔에 출력해줘. 내가 실제 필드명을 확인할 거야
- 에러 시 원문 응답 본문을 그대로 출력하고 종료

파일 하나만 만들고 다른 건 건드리지 마.
```

---

## AI 구조화 추출 + 검수 ★가장 중요

**목표** 비정형 `spcl_cnd` → 정형 조건 배열. 이 서비스의 심장 A.

**할 일**

1. 추출 스크립트 작성 (Claude API 배치)
2. 검증 규칙 코드로 강제 (근거 스팬 부분문자열 체크, 총량 검증)
3. **결과 30~50건 전량을 눈으로 검수.** 자동화하지 말 것. 1시간이면 본다.

**DoD** `data/products.json` 생성, 전 건 `reviewed: true`, 검수 중 발견한 오추출 유형을 메모.

### AI 프롬프트 (스크립트 생성)

```
적금 상품의 우대조건 원문(자연어)을 구조화 JSON으로 변환하는 배치 스크립트를 만들어줘.

파일: scripts/extract-conditions.ts
입력: data/raw/savings.json
출력: data/products.json
모델: Anthropic API, claude-opus-5, temperature 0

각 상품마다 spcl_cnd 원문을 LLM에 보내서 아래 타입 배열을 받아:

type ConditionCode = 'SALARY_TRANSFER' | 'CARD_USAGE' | 'AUTO_TRANSFER'
  | 'FIRST_CUSTOMER' | 'MARKETING_AGREE' | 'NON_FACE_TO_FACE'
  | 'LINKED_PRODUCT' | 'APP_MISSION' | 'OTHER';

interface SpecialCondition {
  code: ConditionCode;
  label: string;      // 사용자에게 보일 한국어 한 줄 설명
  rateBp: number;     // 우대폭, basis point. 0.5%p면 50. 원문에 없으면 0
  evidence: string;   // 원문에서 그대로 복사한 근거 문장. 절대 바꿔쓰지 말 것
  confidence: 'high' | 'low';
}

중요한 검증 로직을 코드로 넣어줘 (LLM에 맡기지 말고):
1. evidence가 원문의 부분 문자열이 아니면 그 조건을 폐기하고 콘솔에 경고
2. G = (intr_rate2 - intr_rate) * 100 (bp). sum(rateBp) > G 이면 모든 조건을
   confidence: 'low'로 바꾸고 needsReview: true 표시
3. unexplainedBp = Math.max(0, G - sum(rateBp)) 를 상품에 기록
4. JSON 파싱 실패 시 1회 재시도, 그래도 실패하면 conditions: [] 로 두고 목록에 기록

마지막에 요약을 출력해: 총 상품 수, needsReview 건수, conditions가 빈 상품 수,
code별 등장 빈도.

프롬프트는 한국어 금융 도메인 문맥을 명확히 주고, JSON 외의 텍스트를 절대
출력하지 않게 지시해줘.
```

### 추출용 시스템 프롬프트 (스크립트 안에 들어갈 내용)

```
당신은 한국 금융상품 공시 문서를 구조화하는 전문가입니다.

주어진 적금 상품의 우대조건 원문을 읽고, 각 우대조건을 아래 8개 코드 중 하나로
분류하고 우대폭을 basis point로 추출하세요.

- SALARY_TRANSFER: 해당 은행으로의 급여이체 실적
- CARD_USAGE: 해당 은행 신용/체크카드 이용 실적
- AUTO_TRANSFER: 공과금·통신비 등 자동이체 등록
- FIRST_CUSTOMER: 신규 고객, 첫 거래, 예적금 미보유
- MARKETING_AGREE: 마케팅 정보 수신 동의
- NON_FACE_TO_FACE: 비대면·모바일앱·인터넷뱅킹 가입
- LINKED_PRODUCT: 청약·펀드·연금·보험 등 타 상품 보유 또는 가입
- APP_MISSION: 앱 출석, 걸음수, 이벤트 미션 등 행동형 조건
- OTHER: 위 어디에도 해당하지 않거나 개인이 통제할 수 없는 조건

규칙
1. evidence 필드에는 원문에서 해당 조건을 서술한 부분을 **글자 그대로** 복사하세요.
   요약하거나 다시 쓰지 마세요.
2. 우대폭이 원문에 명시되지 않았으면 rateBp를 0으로 두세요. 추측하지 마세요.
3. 0.3%p → 30, 0.05%p → 5 로 변환하세요.
4. 조건이 상호 배타적으로 보이면(예: "다음 중 하나") confidence를 'low'로 하세요.
5. JSON 배열만 출력하세요. 설명, 마크다운 코드펜스, 그 외 어떤 텍스트도 금지합니다.
```

### 검수 체크리스트 (직접)

- [ ] rateBp가 원문 숫자와 일치하는가
- [ ] "다음 중 하나만 적용" 같은 배타 조건이 합산되어 있지 않은가
- [ ] `OTHER`로 갔어야 할 조건(예: "만 19세 미만")이 8개 코드에 잘못 매핑되지 않았나
- [ ] evidence가 원문 그대로인가

---

## 계산 엔진 ★가장 중요

**목표** 결정론적 이자 계산 + 랭킹. 서비스의 심장 B. **AI에게 산수를 맡기지 않는다는 서사의 근거.**

**할 일** `lib/interest.ts` + `lib/ranking.ts` + Vitest 테스트

**DoD** 테스트 T1~T7 통과. CLI로 `월 50만 / 12개월 / 조건 2개` 넣으면 랭킹이 출력된다.

### AI 프롬프트

```
적금 이자 계산 엔진을 순수 함수로 만들어줘. UI나 React는 아직 만들지 마.

파일: lib/interest.ts, lib/ranking.ts, 그리고 각각의 Vitest 테스트

lib/interest.ts
  calculateInterest(params: {
    monthlyAmount: number;   // 월 납입액(원)
    months: number;          // 개월
    annualRate: number;      // 연이율, % 단위 (예: 3.5)
    compounding: 'simple' | 'monthly';
  }): { principal, grossInterest, tax, netInterest, maturityAmount }

수식 (정확히 이대로 구현)
  i = annualRate / 100 / 12
  단리:   grossInterest = P * i * n * (n+1) / 2
  월복리: maturityAmount = P * (1+i) * ((1+i)^n - 1) / i
          grossInterest = maturityAmount - P*n
  tax = Math.floor(grossInterest * 0.154)
  netInterest = grossInterest - tax
  반올림은 최종 반환 시 원 단위로 한 번만. 중간 계산은 실수 유지.

lib/ranking.ts
  resolveMyRate(product, option, checkedCodes): { myRate, applied[], missed[] }
    myRate = option.baseRate + sum(해당 상품 조건 중 checkedCodes에 포함된 rateBp)/100
    myRate = Math.min(myRate, option.maxRate)   // 상한 클램프 필수
  rankProducts(products, userInput): 선택 기간 옵션이 있는 상품만 필터하고
    netInterest 내림차순 정렬. 동점이면 baseRate 높은 순.
  simulateAdditions(product, option, checkedCodes): 미충족 조건을 하나씩만
    추가했을 때의 netInterest 증가분을 계산해서 상위 3개 반환.

테스트에 반드시 포함할 케이스
  T1 P=100000 n=12 r=3 단리 → grossInterest === 19500
  T2 P=100000 n=1  r=12 단리 → grossInterest === 1000
  T3 같은 조건에서 월복리 >= 단리
  T4 r=0 → 이자 0, 세금 0
  T5 모든 조건 체크해도 myRate <= maxRate
  T6 조건 0개 → myRate === baseRate
  T7 unexplainedBp>0인 상품은 조건 전부 체크해도 myRate < maxRate

테스트를 먼저 쓰고 그 다음 구현해줘. 통과할 때까지 스스로 고쳐줘.
```

---

## 프론트엔드 단일 페이지

**목표** 입력 → 랭킹 → 상세 → 액션. 화면 하나. 라우팅 없음.

**DoD** 로컬에서 체크박스를 누르면 1위가 실제로 바뀐다.

### AI 프롬프트

```
lib/interest.ts와 lib/ranking.ts는 이미 완성돼 있고 테스트도 통과했어.
이걸 그대로 사용하는 단일 페이지 UI를 만들어줘. 계산 로직은 절대 다시 쓰지 마.

app/page.tsx 하나에 4개 섹션. Tailwind. 모바일 우선.

[A] 입력: 월 납입액(숫자, 천단위 콤마 표시), 기간 셀렉트(6/12/24/36),
    적립방식 라디오, 우대조건 체크박스 8개.
    값이 바뀌면 즉시 재계산. 제출 버튼 없음.
[B] 랭킹 테이블: 순위/은행/상품명/광고 최고금리/내 금리/세후 실수령이자.
    상위 10개. 행 클릭 시 [C] 갱신.
    광고 최고금리 1위와 내 조건 1위가 다르면 테이블 위에 강조 배너:
    "광고 1위와 당신의 1위가 다릅니다"
[C] 상세 카드: 광고금리 → 내 금리를 큰 글씨로 대비. 원금/세후이자/만기수령액.
    충족한 조건(초록)과 미충족 조건(회색)을 우대폭과 함께 나열.
    unexplainedBp > 0이면 회색 안내문.
    "근거 원문 보기" 토글로 spcl_cnd 원문 전체 표시.
    AI가 해석한 조건에는 작은 'AI 해석' 배지.
[D] 액션 리스트: simulateAdditions 결과를 "급여이체를 추가하면 +41,200원" 형태로.

하단 고정 고지: "본 서비스는 금융자문이 아니며, 실제 금리와 조건은 각 금융회사의
상품설명서를 확인하세요. 데이터 출처: 금융감독원 금융상품통합비교공시 (기준: YYYY-MM)"

금액은 전부 천 단위 콤마 + '원'. 금리는 소수점 2자리 + '%'.
숫자가 바뀔 때 시선이 가도록 내 금리와 세후이자만 강조 스타일.
```

---

## 배포 + 선택 기능 + 다듬기

**목표** 심사위원이 접속 가능한 URL 확보. **여기까지가 최소 제출 가능선.**

**할 일**

1. Vercel 배포 (먼저 한다. 배포부터 깨지는 게 흔하다)
2. 데모 시나리오 확정 — "1위가 바뀌는" 입력 조합을 실제로 찾아 고정
3. 여유 있으면 F-11 URL 공유. 자연어 상황 입력은 런타임 LLM 비용과 실패 지점 때문에 제거한다.
4. 빈 상태·에러 상태 처리

**DoD** 공개 URL에서 데모 시나리오가 재현된다. URL 공유 링크가 동작한다.

### AI 프롬프트 (배포 점검)

```
Vercel에 배포하기 전에 점검해줘.
1. data/products.json이 빌드에 포함되는지 확인 (정적 import 또는 public 경로)
2. ANTHROPIC_API_KEY가 클라이언트 번들에 노출되지 않는지 확인.
   LLM 호출은 서버 라우트에서만 일어나야 해
3. npm run build가 통과하는지 확인하고 타입 에러를 고쳐줘
4. 없으면 메타데이터(title, description, og:image) 추가
5. 콘솔에 남은 console.log와 디버그 코드 제거

수정한 항목을 목록으로 알려줘.
```

### 제거된 선택 기능: 자연어 상황 입력

자연어 상황 입력 UI와 전용 서버 파서는 제거한다. RealRate의 생성형 AI 핵심은
우대조건 원문을 배치로 구조화해 런타임 지연, 비용, 실패를 없애는 데 둔다.
사용자 입력은 체크박스 8종 수동 선택으로만 받는다.

---

## 제출 문서

**목표** 기획서 PDF + 기능명세서 PDF. **화면과 문서가 일치하는지가 채점 포인트.**

**할 일**

1. `PLANS/REALRATE_기획서.md`, `PLANS/REALRATE_기능명세서.md`를 실제 구현에 맞게 수정
2. 실제 화면 스크린샷 삽입 (문서에 화면이 없으면 신뢰도가 떨어진다)
3. PDF 변환

**DoD** 문서에 적힌 기능 중 배포 URL에 없는 것이 하나도 없다.

### AI 프롬프트

```
아래 기획서 초안과, 내가 실제로 구현한 기능 목록을 비교해줘.

[기획서 붙여넣기]
[실제 구현된 기능 목록 붙여넣기]

1. 문서에는 있는데 구현되지 않은 기능을 찾아줘 (심사에서 감점되는 지점)
2. 구현했는데 문서에 빠진 기능을 찾아줘 (점수를 놓치는 지점)
3. 문서에서 근거 없이 단정한 문장을 찾아줘
4. "AI를 왜 썼는가"에 대한 설명이 심사위원에게 충분히 설득적인지 평가하고
   부족하면 구체적으로 어떻게 고칠지 제안해줘

고쳐쓰기는 하지 말고 지적만 해줘. 내가 판단할게.
```

---

## 예비일 + 리허설

- 밀린 작업 흡수
- 데모 2분 리허설: 입력 → 결과 전환 → "1위가 바뀐다" → 액션 카드 → 근거 원문
- 다른 브라우저·모바일에서 배포 URL 접속 확인
- **9/7 10:00 마감 / 서비스는 9/11 23:59까지 살아있어야 한다** — 배포 끄지 말 것

---

## 리스크와 대응

| 리스크                                      | 신호       | 대응                                                                                      |
| ------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------- |
| API 필드명이 예상과 다름                    | Day 0      | 실제 응답 기준으로 타입 재정의. 반나절 이내                                               |
| `spcl_cnd`가 비어있거나 무의미한 상품 다수  | Day 0~1    | 저축은행(`030300`) 추가 수집으로 표본 확보                                                |
| 우대폭이 문장에 명시되지 않은 상품이 대부분 | Day 1      | 서사 전환: "명시조차 안 한다"는 것 자체가 문제 제기. `rateBp: 0` + 미해석 우대폭으로 표시 |
| Day 3 UI가 안 끝남                          | Day 3 저녁 | 랭킹 테이블 + 상세 카드만 남기고 액션 리스트를 텍스트 한 줄로 축소                        |
| 전면 지연                                   | Day 4      | 런타임 AI 선택 기능과 부가 기능을 줄인다. 배포 URL이 최우선                               |

---

## AI에게 지시할 때의 공통 규칙

1. **파일 하나씩.** "전체 앱을 만들어줘"는 안 된다.
2. **기존 파일을 다시 쓰지 말라고 명시한다.** 특히 검증 끝난 `lib/interest.ts`.
3. **테스트를 먼저 요구한다.** 계산 로직은 특히.
4. **"추측하지 말고 모르면 0으로 두라"**를 데이터 추출 프롬프트에 항상 넣는다. 금융에서 그럴듯한 추측은 거짓말이다.
5. **AI가 만든 우대조건 추출 결과와 계산 결과는 직접 확인한다.** 나머지는 맡겨도 된다.

---

# 개정 v2 — 2026-08-31, 공식 제출 템플릿 반영

공식 `첨부1 기획서` / `첨부2 기능명세서` 템플릿을 확인하고 아래 3가지를 변경했다.
템플릿에 맞춰 채운 최종본은 `OUTBOX/2026_금융AI_기획서_REALRATE.md`,
`OUTBOX/2026_금융AI_기능명세서_REALRATE.md`.

## 변경 1 — 생성형 AI 기능 범위 재정의

템플릿 기획서 5번이 **"생성형 AI 모델을 서비스 내에서 어떻게 활용했고 어떤 역할을 수행하는지"**를
필수항목으로 요구한다. 다만 런타임 자연어 상황 입력은 배치 추출 중심 제품 서사와 충돌하고,
지연·비용·장애 지점을 늘리므로 제거한다.

- **자연어 상황 입력** — 선택 기능이었으나 **제거**
- **F-11 결과 해설 + 가입 전 확인 질문 3개 생성** — 신규 추가, **필수**
  (사용자가 "AI 해설 보기"를 누를 때만 호출 → 지연·비용·실패가 사용자 기대 안에 들어옴)

F-11은 계산 결과 수치를 **인용만** 하고 새로 산출하지 않는다. 프롬프트에 명시할 것.

## 변경 2 — Vercel 초기 배포를 Day 0 끝으로 이동

배포 리스크를 마지막 날까지 끌고 가지 않는다. Day 0에 빈 껍데기라도 올려서
도메인·빌드·환경변수 경로를 미리 확보한다. 15분이면 된다.

## 변경 3 — 일정 재배치

| 날         | 변경 후                                                  |
| ---------- | -------------------------------------------------------- |
| 8/31 Day 0 | 인증키 + 데이터 수집 **+ Vercel 빈 프로젝트 배포(15분)** |
| 9/1 Day 1  | AI 구조화 추출 + 전량 검수 (변경 없음)                   |
| 9/2 Day 2  | 계산 엔진 + 테스트 (변경 없음)                           |
| 9/3 Day 3  | 프론트 단일 페이지 **+ 그날 바로 배포 반영**             |
| 9/4 Day 4  | **F-11 생성형 AI 해설**, 공유 링크, 다듬기               |
| 9/5 Day 5  | 템플릿 양식에 채워 넣기 + 스크린샷 + PDF                 |
| 9/6 Day 6  | 예비일 + 데모 리허설                                     |

## Day 4 추가 프롬프트 — F-11 결과 해설 생성

```
계산 결과를 설명하는 생성형 AI 기능을 추가해줘. 계산은 이미 끝나 있고,
AI는 설명만 해. 숫자를 새로 만들면 안 돼.

- app/api/explain/route.ts : POST { product, myRate, netInterest, applied[], missed[], rawSpecialCondition }
  → { explanation: string, questions: string[3] }
- claude-sonnet-5, temperature 0.3, 타임아웃 8초
- 시스템 프롬프트에 반드시 넣을 것:
  · 주어진 금액과 금리 수치를 그대로 인용하세요. 새로운 숫자를 계산하거나
    추정하지 마세요.
  · 투자 권유나 가입 권유 표현을 쓰지 마세요. "~하는 것이 유리할 수 있습니다"
    수준의 사실 서술만 하세요.
  · questions는 사용자가 은행에 직접 물어봐야 할 구체적 질문 3개입니다.
    (예: 급여이체 우대는 몇 개월 유지해야 인정되나요?)
- 프론트: 상세 카드의 "AI 해설 보기" 버튼을 눌렀을 때만 호출.
  로딩 상태 표시, 실패 시 정적 기본 문구로 폴백하고 버튼은 다시 시도 가능하게.
- IP당 분 10회 레이트 리밋
```

## 문서 작성 시 주의 (템플릿 대응)

- 기획서 3번은 **고객만이 아니라 "채널"** 선택 이유도 필수다. 모바일 웹(비대면)을 고른
  이유를 반드시 쓸 것 — 비대면 가입이 그 자체로 우대조건이라는 논리가 강하다.
- 기획서 6번은 **금융 외 영역 확장 가능성**까지 요구한다. "조건부 혜택 구조의 일반화"로
  통신 요금제·공공 지원금·구독 서비스까지 확장 서술.
- 기능명세서 1번은 **"미구현 또는 향후 구현 예정 기능은 제외"**라고 못박혀 있다.
  로드맵을 여기에 쓰면 감점이다. 로드맵은 기획서 6번에만 쓴다.
- 기능명세서 2번의 **구현 상태 열은 제출 직전에 실제 화면과 대조**할 것.
  문서에만 있고 화면에 없는 기능이 가장 큰 감점 요인이다.
- 기능명세서 5번은 **심사자가 그대로 따라 할 수 있는 샘플 입력값과 예상 결과**를 요구한다.
  "순위가 바뀌는" 입력 조합을 Day 4에 실제로 찾아서 문서에 고정할 것.

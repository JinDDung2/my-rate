---
title: "RealRate 기능명세서 — 2026 금융 AI Challenge 제출용"
tags: [finance-ai, hackathon, spec, realrate]
status: active
created: 2026-08-31
---

# 진짜금리(RealRate) 기능명세서

> 제출 문서 2: 기능명세서 (PDF 변환 대상)

---

## 1. 범위

| 항목 | 내용 |
|---|---|
| 대상 상품군 | 적금 (정기예금·대출 제외) |
| 대상 기관 | 은행 (`topFinGrpNo=020000`) — 여유 시 저축은행 `030300` 추가 |
| 상품 수 | 상위 30~50개 |
| 사용자 계정 | 없음 (로그인·회원가입·개인정보 저장 없음) |
| 상태 저장 | 브라우저 메모리만. 서버 저장 없음 |

---

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| 기본금리 | 우대조건 없이 적용되는 금리 (`intr_rate`) |
| 최고우대금리 | 모든 우대조건 충족 시 금리 (`intr_rate2`) |
| 우대갭(G) | `최고우대금리 − 기본금리` |
| 내 금리(My Rate) | 기본금리 + 사용자가 충족한 조건의 우대폭 합 |
| 실수령 이자 | 내 금리 기준 만기 이자에서 이자소득세 15.4%를 차감한 금액 |
| 미해석 우대폭 | 우대갭 중 AI가 조건으로 귀속시키지 못한 잔여분. **계산에 미반영** |

---

## 3. 표준 우대조건 코드 (8종)

| 코드 | 라벨 | 체크박스 문구 |
|---|---|---|
| `SALARY_TRANSFER` | 급여이체 | 이 은행으로 급여를 받고 있어요 |
| `CARD_USAGE` | 카드 실적 | 이 은행 카드를 월 30만원 이상 써요 |
| `AUTO_TRANSFER` | 자동이체 | 공과금·통신비 자동이체를 걸 수 있어요 |
| `FIRST_CUSTOMER` | 첫 거래 | 이 은행과 첫 거래예요 |
| `MARKETING_AGREE` | 마케팅 동의 | 마케팅 정보 수신에 동의할 수 있어요 |
| `NON_FACE_TO_FACE` | 비대면 가입 | 앱·인터넷으로 가입할 거예요 |
| `LINKED_PRODUCT` | 연계상품 보유 | 청약·펀드·연금 등 다른 상품이 있어요 |
| `APP_MISSION` | 앱 미션/출석 | 앱 출석·미션 같은 이벤트를 수행할 수 있어요 |
| `OTHER` | 기타(계산 제외) | (체크 불가, 정보 표시용) |

`OTHER`로 분류된 조건은 **내 금리에 절대 가산하지 않는다.**

---

## 4. 화면 구성 (단일 페이지, 4개 섹션)

```
┌─ [A] 입력 패널 ────────────────────────────┐
│  월 납입액 [500,000] 원                     │
│  가입 기간 [12] 개월                        │
│  적립 방식 (•) 정액적립  ( ) 자유적립        │
│  ── 내가 충족할 수 있는 조건 ──              │
│  [v] 비대면 가입   [ ] 급여이체              │
│  [v] 마케팅 동의   [ ] 카드 실적  ... (8개)  │
│  ( 자연어로 입력하기 ▸ 선택 기능 )           │
└──────────────────────────────────────────┘
┌─ [B] 결과 랭킹 테이블 ─────────────────────┐
│ 순위 | 은행 | 상품명 | 광고금리 | 내금리 | 세후실수령 │
└──────────────────────────────────────────┘
┌─ [C] 1위 상품 상세 카드 ──────────────────┐
│  광고 최고금리 7.00%  →  내 금리 2.80%      │
│  원금 6,000,000 + 세후이자 76,986          │
│  충족 O: 비대면(0.1%p), 마케팅(0.1%p)       │
│  미충족 X: 급여이체(0.5%p), 카드(0.3%p)     │
│  미해석 우대폭 3.5%p (미반영)               │
│  [근거 원문 보기 ▾]                         │
└──────────────────────────────────────────┘
┌─ [D] 액션 리스트 ──────────────────────────┐
│  급여이체를 추가하면  +41,200원             │
│  카드실적을 추가하면  +24,700원             │
└──────────────────────────────────────────┘
  ⚠ 본 서비스는 금융자문이 아닙니다 / 데이터 기준: 2026-08
```

---

## 5. 기능 목록

### F-01 상품 데이터 로드
- **입력** 없음 (페이지 진입)
- **처리** `data/products.json` 로드
- **출력** 상품 목록, 데이터 기준월 표시
- **예외** 로드 실패 시 "데이터를 불러오지 못했습니다" + 재시도 버튼

### F-02 조건 입력
- **입력** 월 납입액(10,000~1,000,000, 만원 단위 스텝), 기간(6/12/24/36개월), 적립방식, 조건 체크박스 8종
- **처리** 입력 즉시(onChange) 재계산 — 제출 버튼 없음
- **예외** 범위 밖 값 입력 시 인라인 경고, 계산은 직전 유효값 유지

### F-03 내 금리 산정
- **입력** 상품 1건 + 사용자 조건 집합
- **처리**
  ```
  myRate = baseRate + Σ(사용자가 체크한 조건 중 해당 상품이 제공하는 조건의 rate_bp)
  단, myRate는 maxRate(intr_rate2)를 초과할 수 없다 (상한 클램프)
  ```
- **출력** 내 금리(%), 적용 조건 목록, 미적용 조건 목록

### F-04 이자 계산 엔진
- **입력** 월 납입액 `P`, 기간 `n`(개월), 연이율 `r`, 이자유형(단리/월복리)
- **처리** §7 수식
- **출력** 원금, 세전이자, 세금, 세후이자, 만기수령액
- **제약** 부동소수점 오차 방지를 위해 원 단위 반올림은 **최종 1회만**

### F-05 실수령 기준 랭킹
- **입력** 전체 상품 × 사용자 조건
- **처리** 사용자가 선택한 기간(`save_trm`) 옵션이 존재하는 상품만 필터 → 세후이자 내림차순 정렬
- **출력** 순위 테이블 (동점 시 기본금리 높은 순)

### F-06 광고금리 vs 내 금리 대비 표시
- **출력** 최고우대금리 기준 1위 상품과 내 조건 기준 1위 상품이 **다를 경우 배지로 강조** ("광고 1위와 다릅니다")

### F-07 조건 충족 시뮬레이션 (액션 리스트)
- **처리** 미충족 조건 각각을 단독으로 추가 충족했다고 가정하고 F-03~F-04 재실행 → 세후이자 증가분 계산
- **출력** 증가액 내림차순 상위 3개
- **제약** 조합 폭발 방지를 위해 **단일 조건 추가만** 시뮬레이션

### F-08 근거 원문 표시
- **출력** 각 조건 카드 하단에 `evidence` 스팬 + 상품 전체 `spcl_cnd` 원문 토글
- **표기** AI가 해석한 항목에는 `AI 해석` 배지 표시

### F-09 미해석 우대폭 고지
- **처리** `G − Σ(추출된 조건 rate_bp)` > 0.05%p 이면 표시
- **출력** "공시상 최대 N%p의 추가 우대가 있으나 조건을 특정할 수 없어 반영하지 않았습니다"

### F-10 (선택) 자연어 상황 입력
- **입력** 자유 텍스트 1~3문장
- **처리** LLM 1회 호출 → 8개 조건 코드 boolean 매핑
- **출력** 체크박스 자동 체크 + "AI가 이렇게 이해했어요" 요약
- **예외** 실패/타임아웃(5초) 시 조용히 수동 입력으로 폴백, 에러 토스트 1회

### F-11 결과 공유
- **처리** 입력 상태를 URL 쿼리스트링으로 직렬화
- **출력** 복사 가능한 링크 (심사위원 재현성 확보용)

### F-12 고지 및 면책
- 전 페이지 하단 고정: 금융자문 아님 / 데이터 출처·기준월 / 월 단위 근사 계산 안내

---

## 6. 데이터 모델

```ts
type ConditionCode =
  | 'SALARY_TRANSFER' | 'CARD_USAGE' | 'AUTO_TRANSFER' | 'FIRST_CUSTOMER'
  | 'MARKETING_AGREE' | 'NON_FACE_TO_FACE' | 'LINKED_PRODUCT' | 'APP_MISSION'
  | 'OTHER';

interface SpecialCondition {
  code: ConditionCode;
  label: string;        // 사용자에게 보일 짧은 설명
  rateBp: number;       // 우대폭 (basis point, 50 = 0.5%p). 불명 시 0
  evidence: string;     // 공시 원문에서 그대로 잘라낸 근거 문장
  confidence: 'high' | 'low';
}

interface RateOption {
  saveTrm: number;              // 개월
  intrRateType: 'S' | 'M';      // S=단리, M=월복리
  rsrvType: 'S' | 'F';          // S=정액적립, F=자유적립
  baseRate: number;             // % (intr_rate)
  maxRate: number;              // % (intr_rate2)
}

interface Product {
  finPrdtCd: string;
  companyName: string;          // kor_co_nm
  productName: string;          // fin_prdt_nm
  joinWay: string;              // join_way
  rawSpecialCondition: string;  // spcl_cnd 원문 (반드시 보존)
  conditions: SpecialCondition[];
  options: RateOption[];
  disclosureMonth: string;      // dcls_month
  unexplainedBp: number;        // 미해석 우대폭
  reviewed: boolean;            // 사람 검수 완료 여부
}
```

---

## 7. 계산 규칙 명세

기호: `P` 월 납입액, `n` 개월 수, `r` 연이율(소수), `i = r / 12`, 세율 `t = 0.154`

### 7.1 정액적립식 · 단리
```
원금     = P × n
세전이자 = P × i × n(n+1) / 2
```

### 7.2 정액적립식 · 월복리
```
만기금액 = P × (1+i) × ((1+i)^n − 1) / i
세전이자 = 만기금액 − P × n
```

### 7.3 세금
```
세금     = floor(세전이자 × 0.154)
세후이자 = 세전이자 − 세금
```

### 7.4 자유적립식
MVP에서는 사용자가 입력한 월 납입액을 매월 정액 납입한다고 가정하고 7.1/7.2를 그대로 적용한다. 화면에 가정을 명시한다.

### 7.5 검증 테스트 케이스 (필수 구현)
| 케이스 | 입력 | 기대 |
|---|---|---|
| T1 | P=100,000 n=12 r=0.03 단리 | 세전이자 19,500 |
| T2 | P=100,000 n=1 r=0.12 단리 | 세전이자 1,000 |
| T3 | 단리 vs 월복리 동일 r | 복리 ≥ 단리 |
| T4 | r=0 | 이자 0, 세금 0 |
| T5 | myRate 상한 클램프 | myRate ≤ maxRate |
| T6 | 조건 0개 선택 | myRate == baseRate |
| T7 | 조건 전부 선택 + unexplainedBp>0 | myRate < maxRate |

---

## 8. AI 파이프라인 명세

### 8.1 배치 추출 (빌드 타임 1회)
| 항목 | 값 |
|---|---|
| 모델 | `claude-opus-5` (정확도 우선. 대량 시 `claude-sonnet-5`) |
| 입력 | `spcl_cnd` 원문 1건 + 기본금리/최고우대금리 |
| 출력 | `SpecialCondition[]` (JSON only) |
| 온도 | 0 |
| 재시도 | JSON 파싱 실패 시 1회 재시도 후 수동 처리 큐로 |

### 8.2 출력 검증 규칙 (코드로 강제)
1. `evidence` 문자열이 `rawSpecialCondition`의 **부분 문자열이 아니면 폐기** (환각 차단)
2. `Σ rateBp > G × 100` 이면 전부 `confidence: 'low'` 처리 후 수동 검수 큐로
3. `rateBp === 0` 인 조건은 표시만 하고 계산에서 제외
4. `unexplainedBp = max(0, G×100 − Σ rateBp)`
5. 전 상품 검수 완료(`reviewed: true`) 전에는 배포 빌드 실패

### 8.3 런타임 (선택 기능 F-10)
| 항목 | 값 |
|---|---|
| 모델 | `claude-sonnet-5` |
| 호출 위치 | 서버 라우트 `/api/parse-situation` (API 키 서버 보관) |
| 타임아웃 | 5초, 실패 시 폴백 |
| 레이트 리밋 | IP당 분 10회 |

---

## 9. API 명세

| 메서드 | 경로 | 요청 | 응답 |
|---|---|---|---|
| GET | `/api/products` | — | `{ disclosureMonth, products: Product[] }` |
| POST | `/api/parse-situation` | `{ text: string }` | `{ conditions: ConditionCode[], summary: string }` |

이자 계산 및 랭킹은 클라이언트의 순수 모듈 `lib/interest.ts`에서 수행한다(네트워크 왕복 없음). 동일 모듈을 테스트에서 직접 호출한다.

---

## 10. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 성능 | 조건 변경 → 결과 갱신 200ms 이내 (런타임 LLM 없음) |
| 가용성 | 2026-09-07 11:00 ~ 2026-09-11 23:59 무중단 접근 (대회 요건) |
| 보안 | 개인정보 미수집·미저장. API 키는 서버 환경변수. 클라이언트 노출 금지 |
| 접근성 | 모바일 우선 반응형, 명도 대비 WCAG AA, 금액은 천 단위 구분 |
| 투명성 | 모든 AI 해석 항목에 원문 근거 병기, 데이터 기준월 상시 노출 |
| 로깅 | 사용자 입력값 서버 로깅 금지 |

---

## 11. 완료 정의 (Definition of Done)

- [ ] 배포 URL에서 조건 변경 시 랭킹이 실시간으로 바뀐다
- [ ] 광고 1위 ≠ 내 조건 1위인 케이스가 데모 입력에서 실제로 발생한다
- [ ] 계산 엔진 테스트 T1~T7 전부 통과
- [ ] 전 상품 우대조건 추출 결과 사람 검수 완료
- [ ] 모든 조건 카드에 공시 원문 근거가 붙는다
- [ ] 금융자문 아님 고지가 전 화면에 노출된다
- [ ] 기획서·기능명세서 PDF와 실제 화면 기능이 일치한다

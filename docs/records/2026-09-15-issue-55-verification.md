# Issue #55 검증

## 구현

- `pr-54` (`4148b99`) 기반으로 구현했다.
- 조건 3구획을 `ProductConditions`로 추출해 1위 카드와 랭킹 테이블에서 공유한다. 기존 카드의 HTML 구조·클래스·문구를 유지했다.
- 미확인 조건이 있는 각 상품에 개수 배지와 조건 확인 진입점을 추가했다. 상세는 `tbody` 내부 별도 `tr > td colSpan={6} > details`에 둔다.
- 네이티브 `details`를 사용하므로 추가 React 상태나 훅이 필요 없다. 상품·기간·적립방식 키의 Fragment가 재정렬 시 펼침을 해당 상품에 유지한다. 미확인 0개가 되면 상세를 제거한다.
- 기존 상품별 확인 콜백을 연결했다. 계산 로직 4개 파일은 변경하지 않았다.
- 기능명세서 §4 [B] 와이어프레임과 동작 설명을 갱신했다.

## 자동 검증

- `npm run typecheck`: 통과.
- `npm test`: 76개 통과, 실패 0개. 기존 `top-product-card.test.ts` 수정 없이 통과.
- 새 `ranking-table.test.ts`: 정적 HTML의 배지·3구획·테이블 구조·체크박스 범위 검증. 실제 입력 핸들러 호출로 상품별 override, 2위→1위 역전, 상세 카드 갱신, 같은 은행 및 다른 은행 상품 불변 검증.
- `git diff --check`: 통과.

## Chrome UI 시나리오 검증

임시 Chrome 프로필에서 API 응답을 테스트 상품으로 대체했다. 저장소 상품 데이터는 수정하지 않았다.

- A: 기본 4%, 미확인 없음. B: 기본 3%, 급여 +2%p·카드 +0.2%p 미확인. C는 B와 같은 은행, D는 다른 은행.
- B(2위)의 `미확인 2` 배지 확인, B·C 상세를 동시에 펼침.
- B 급여 체크 → B 5%로 1위, 1위 카드 B로 갱신. B·C의 펼침 상태 유지.
- A·C·D의 내금리와 세후실수령은 확인 전후 동일.
- 새 1위 B의 카드 조건 체크 → 마지막 미확인 소진, B 배지 및 펼침 영역 제거. 최초 A도 배지 없음.
- 390px 모바일 화면에서 문서 가로 넘침 없음.
- 실행 스크립트 `/tmp/issue55-browser.mjs`, 화면 `/tmp/issue55-browser.png` (임시 검증 산출물).

기본 Turbopack 개발 서버는 worktree 밖 의존성 경로를 해석하지 못했다. `npm run dev -- --webpack --port 3055` 및 `http://localhost:3055`로 검증을 완료했다. 프로덕션 빌드는 실행하지 않았다.
